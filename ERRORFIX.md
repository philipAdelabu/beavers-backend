## Error fixes and important installations

This section is meant to described the errors at the installation level.

### PostGis error

To fix this error the following steps were taken:
On a linux system system install the following.

sudo apt-get update
sudo apt-get install postgresql-16-postgis-3 postgresql-16-postgis-3-scripts

Of course, postgresql server should be installed.
