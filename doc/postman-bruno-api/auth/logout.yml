info:
  name: logout
  type: http
  seq: 7

http:
  method: POST
  url: 127.0.0.1:3000/api/v1/auth/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
